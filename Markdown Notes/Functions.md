* Need a prototype at the top
	* Consists of return type, name and input
```c
float c_to_f(float)
```
* Call the function
```c
c_to_f(var)
```
* Function
```c
float c_to_f(float) {
	..........
	return
} 
```